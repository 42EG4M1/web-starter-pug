include _settings
doctype html
html(lang="ja")
  block head
  include ../components/_head

  body
    noscript(class="noscript" data-noscript="本サイトは JavaScript が有効な状態にないと正常に表示されません。このメッセージが表示される場合、ブラウザの設定状況をご確認ください。")
    include _ie
    include ../components/_header
    block contents
    include ../components/_footer
    script(charset="utf-8" src=`./js/bundle.min.js?ver=${version}`)